spring:
  flyway:
    enabled: true
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    properties.hibernate:
      hbm2ddl.auto: create-drop